services:
  telegram_bot:
    build: ./tg_bot
    container_name: telegram_bot
    networks:
      - gypys_sila

  streamlit_app:
    build: ./st_project
    container_name: streamlit_web
    ports:
      - "8501:8501"
    networks:
      - gypys_sila

networks:
  gypys_sila:
    driver: bridge
